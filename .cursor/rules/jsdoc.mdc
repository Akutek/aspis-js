---
description: JSDoc ist Typ-SSOT und Human UX
globs: **/*.js
alwaysApply: false
---

# JSDoc

Typen und geteilte `@typedef`s liegen in JSDoc (`src/types`, Methodensignaturen). Prüfung über `jsconfig.json` (`checkJs`). Kein `types.d.ts`. Die Regel: `ssot-types.mdc`.
