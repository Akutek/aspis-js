{
    "name": "form-raid",
    "placeholder": {
        "action": "{{action}}",
        "csrf": "{{csrf}}",
        "submitLabel": "{{submitLabel}}"
    },
    "files": {
        "markup": "form-raid.html"
    }
}
